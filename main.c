#include <stdio.h>
#include <stdlib.h>

int main()
{
    int qtd, n, i;
    scanf("%i %i", &qtd, &n);

    for ()
    while(qtd
    {
        if (n == 0)
        {
            printf("NULL\n");
        }
        else if (n > 0)
        {
            printf("ODD POSITIVE\n");
        }
        else
        {
            printf("ODD NEGATITIVE\n");
        }
        scanf("%i", &n);
    }
    system("pause");
    return 0;
}
